

<div class="container-fluid text-center">

    <div class="container text-secondary m-1 fs-6 mb-2">
        <div class="row align-items-center justify-content-center">
            <div class="col p-0 m-0">
                <i class="fa fa-message"></i>
                <span> messages </span>
                <span data-controller="counter" data-target-value="<%= @messages.count %>"></span>
            </div>
        </div>
    </div>

    <div class="container text-secondary m-1 fs-6">
        <div class="row align-items-center justify-content-center">
            <div class="col p-0 m-0">
                <i class="fa fa-user-group"></i>
                <span> utilisateurs </span>
                <span data-controller="counter" data-target-value="<%= @users.count %>"></span>
            </div>
        </div>
    </div>

</div>